[project]
name = "sql2neo4j"
version = "0.1.0"
description = "Relational (SQLite) to Neo4j graph mapper"
dependencies = [
    "neo4j>=5.0"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
